version: '3'
services:
  emu:
    buildout: .
    image: birdhouse/{{ cookiecutter.project_slug }}
    environment:
      HOSTNAME: $HOSTNAME
      HTTP_PORT: $HTTP_PORT
      OUTPUT_PORT: $OUTPUT_PORT
    ports:
      - "$HTTP_PORT:$HTTP_PORT"
      - "$OUTPUT_PORT:$OUTPUT_PORT"
